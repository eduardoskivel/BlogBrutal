---
import Layout from '../layouts/Default.astro';
import Card from '../components/generic/Card.astro';
import Button from '../components/generic/Button.astro';
import Pill from '../components/generic/Pill.astro';
import BlogList from '../components/blog/BlogList.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog').then((p) => p.slice(0, 3));
---

<Layout title="Riviera by Eduardo | Diario de Vida y Territorio">

  <main class="bg-white p-6">

    <!-- HERO PERSONAL -->
    <section class="grid md:grid-cols-8 gap-8 mt-4">
      <div class="col-span-6">
        <Card>
          <div class="flex flex-col gap-4">
            <h1 class="text-5xl md:text-6xl font-bold leading-tight">
              Vivir con intención en la Riviera Maya
            </h1>
            <p class="text-xl text-gray-700">
              Documentando mi camino como desarrollador, triatleta, curioso de la mente humana, y explorador del Caribe mexicano.
            </p>
            <div class="flex gap-4 mt-4">
              <Button href="/blog" theme="secondary">Leer el Blog</Button>
              <Button href="/sobre-mi" theme="primary">Sobre mí</Button>
            </div>
          </div>
        </Card>
      </div>
    </section>

    <!-- TEMAS CLAVE (NO-COMERCIALES) -->
    <section class="mt-12">
      <h2 class="text-3xl font-bold mb-6">Lo que me mueve</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <Card>
          <div class="p-6">
            <span class="text-4xl">🌴</span>
            <h3 class="text-2xl font-bold mt-4">Riviera y Naturaleza</h3>
            <p class="mt-2">Espacios para respirar, reconectar y crear comunidad.</p>
            <a href="/naturaleza" class="inline-block mt-4 font-bold">Leer más →</a>
          </div>
        </Card>
        <Card>
          <div class="p-6">
            <span class="text-4xl">🧠</span>
            <h3 class="text-2xl font-bold mt-4">Neurodiversidad</h3>
            <p class="mt-2">Una forma distinta de habitar el mundo y el tiempo.</p>
            <a href="/neurodiversidad" class="inline-block mt-4 font-bold">Explorar →</a>
          </div>
        </Card>
        <Card>
          <div class="p-6">
            <span class="text-4xl">👴</span>
            <h3 class="text-2xl font-bold mt-4">Vida Larga</h3>
            <p class="mt-2">Diseño humano para quienes viven con intención, a cualquier edad.</p>
            <a href="/adultos-mayores" class="inline-block mt-4 font-bold">Descubrir →</a>
          </div>
        </Card>
      </div>
    </section>

    <!-- ÚLTIMOS POSTS -->
    <section class="mt-16">
      <h2 class="text-3xl font-bold mb-6">Últimos escritos</h2>
      <BlogList posts={posts} />
      <div class="mt-4">
        <Button href="/blog" theme="secondary">Ver todos los artículos</Button>
      </div>
    </section>

    <!-- MANIFIESTO -->
    <section class="mt-20 p-8 bg-nature-100 rounded-xl">
      <h2 class="text-3xl font-bold mb-4">¿Qué hago aquí?</h2>
      <p class="text-lg leading-relaxed max-w-2xl">
        Creo que desarrollar tierra no es solo construir casas. Es construir posibilidades.  
        Espacios donde las personas puedan ser libres, vivir con dignidad, y tener comunidad.  
        Esto es mi diario abierto sobre ese camino.
      </p>
    </section>

    <!-- CONTACTO NO-INVASIVO -->
    <section class="mt-16 text-center">
      <p class="text-xl mb-4">¿Te gustaría conversar sobre vivir aquí o colaborar?</p>
      <Button href="/contacto" theme="primary">Escríbeme</Button>
    </section>

  </main>
</Layout>
